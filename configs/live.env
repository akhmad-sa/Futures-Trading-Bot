# Live / paper trading (portfolio engine)
# Paper: simulated orders + live OHLCV when the venue has no ccxt testnet.
# API keys for the active exchange are still used for market data.
# Virtual balance: BACKTEST_INITIAL_CAPITAL in configs/backtest.env

# Reuse portfolio slot model from backtest
PORTFOLIO_MAX_CONCURRENT_SYMBOLS=true
MAX_CONCURRENT_TRADES=1
