#cloud-config
# Contabo / generic VPS — same dynamic layout as contabo-init.sh

runcmd:
  - [ bash, -lc, "curl -fsSL https://raw.githubusercontent.com/akhmad-sa/Futures-Trading-Bot/main/deploy/contabo-init.sh | bash" ]
