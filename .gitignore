# ── Secrets & local env (never commit) ────────────────────────────
.env
.env.local
.env.*.local
!.env.example

# ── AI assistants (aider, etc.) — not project source ──────────────
.aider/
.aider*
.aider.tags.cache.v4/

# ── Editor / IDE ──────────────────────────────────────────────────
.cursor/
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Python virtualenv (never commit — recreate with python -m venv) ─
venv/
.venv/
**/venv/
**/.venv/
env/
ENV/
.python-version

# ── Python bytecode & packaging ───────────────────────────────────
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
*.egg
*.whl
*.tar.gz
pip-wheel-metadata/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
.hypothesis/
.tox/
.nox/
coverage.xml
*.cover

# ── Logs & backtest / run output ──────────────────────────────────
logs/
*.log
output*.log

# ── Local market data (Parquet candles) ───────────────────────────
data/candles/
**/*.parquet

# Legacy data path (removed from repo)
data_source/

# ── SQLite runtime (keep storage/*.py in repo) ────────────────────
storage/*.db
storage/*.db-shm
storage/*.db-wal
storage/*.sqlite
storage/*.sqlite3
*.sqlite3

# ── OS / temp ─────────────────────────────────────────────────────
*.bak
tmp/
temp/
.cache/

# ── Keys & certs (accidental upload) ──────────────────────────────
*.pem
*.key
*.p12
credentials.json
secrets.json

# ── Accidental paste / editor junk at repo root ───────────────────
return payment
self._equity_curve*
self.close_commission*
